meta {
  name: Auth Login (XSS)
  type: http
  seq: 1
}

post {
  url: http://localhost:8080/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "user@example.com",
    "password": "user@123",
    "deviceId": "<script>alert('xss device ID')</script>bruno-test-device",
    "deviceName": "<img src=x onerror=alert('xss deviceName')>Bruno API Tester"
  }
}

vars:post-response {
  access_token: res.body.accessToken
}
