meta {
  name: Auth Logout
  type: http
  seq: 3
}

post {
  url: http://localhost:8080/api/auth/logout
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "deviceId": "bruno-test-device"
  }
}
